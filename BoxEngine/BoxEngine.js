import { Box } from "../Box/box.js";
import {
  addScenes,
  switchScenes,
  set,
  cameraShake,
  zoom,
  save,
  pause,
  play,
} from "../globals/globals.js";

const BoxEngine = {
  Box,
  addScenes,
  switchScenes,
  set,
  cameraShake,
  zoom,
  save,
  pause,
  play,
};

export default BoxEngine;
