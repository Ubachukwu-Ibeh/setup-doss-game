import { Box } from "../Box/box.js";
import {
  addScenes,
  switchScenes,
  setGame,
  setCamera,
} from "../globals/globals.js";

const BoxEngine = {
  Box,
  addScenes,
  switchScenes,
  setCamera,
  setGame,
};

export default BoxEngine;
