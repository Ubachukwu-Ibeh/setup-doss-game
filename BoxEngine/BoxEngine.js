import { Box } from "../Box/box.js";
import {
  addScenes,
  switchScenes,
  set,
  cameraShake,
  zoom,
} from "../globals/globals.js";

const BoxEngine = {
  Box,
  addScenes,
  switchScenes,
  set,
  cameraShake,
  zoom,
};

export default BoxEngine;
