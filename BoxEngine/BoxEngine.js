import { Box } from "../Box/box.js";
import {
  addScenes,
  switchScenes,
  set,
  cameraShake,
  zoom,
  save,
  pause,
  play,
  rotate,
  preloadImage,
  preloadSound,
} from "../globals/globals.js";

const BoxEngine = {
  Box,
  addScenes,
  switchScenes,
  set,
  cameraShake,
  zoom,
  save,
  pause,
  play,
  rotate,
  preloadImage,
  preloadSound,
};

export default BoxEngine;
