import { Box } from "../Box/box.js";
import {
  addScenes,
  switchScenes,
  set,
  cameraShake,
} from "../globals/globals.js";

const BoxEngine = {
  Box,
  addScenes,
  switchScenes,
  set,
  cameraShake,
};

export default BoxEngine;
